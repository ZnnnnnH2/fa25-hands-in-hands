// 尝试计算 0 到 n 中每个数字出现的次数 - 0 到 m-1 中每个数字出现的次数

#include <math.h>
#include <stdio.h>
#include <string.h>

#include <algorithm>
#include <iostream>
#include <vector>

typedef long long ll;
typedef int* intt;
typedef char* charr;

const int inf = 0x3f3f3f3f;
const int N = 1e5 + 5;
ll countm[11], countn[11];
int m, n;
ll* countNumber(int num, ll* count) {
  ll tmp[11];
  memset(tmp, 0, sizeof(tmp));
  
}
int main() {
  return 0;
}